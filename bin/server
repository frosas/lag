#!/bin/bash

set -ex

source $(dirname $0)/common

on_sigint() { kill 0; }
trap on_sigint SIGINT

node_modules/.bin/forever $([[ $is_dev ]] && echo '-w') src/server/http.js &
if [[ $is_dev ]]; then webpack --watch & fi

wait